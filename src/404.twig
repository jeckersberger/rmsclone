{% extends "assets/template.twig" %}

{% block content %}
    <div class="error-page">
        <h2 class="headline text-warning"> 404</h2>

        <div class="error-content">
            <h3><i class="fas fa-exclamation-triangle text-warning"></i> Seite nicht gefunden.</h3>
            <p>
                Die angeforderte Seite konnte nicht gefunden werden.
                <br/>Zurueck zum <a href="{{ CONFIG.ROOTURL }}">Dashboard</a>{% if USERDATA.instances|length > 1 %}, oder wechseln Sie zu einem anderen Unternehmen:{% endif %}
            </p>
            {% if USERDATA.instances|length > 1 %}
            <ul>
                {% for instance in USERDATA.instances %}
                    {% if USERDATA.instance.instances_id != instance.instances_id %}
                        <li>
                            <a href="{{ CONFIG.ROOTURL }}?i={{instance.instances_id}}"  class="changeInstanceActionButton" data-newinstance="{{instance.instances_id}}">
                                {{instance.instances_name}}
                            </a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
            {% endif  %}
        </div>
    </div>
    
{% endblock %}
